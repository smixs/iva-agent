// Доля окна модели, на которой eve пересказывает историю сессии (компактация). Ива держит
// её ниже дефолтных 0.9, оставляя запас на summary-вызов и следующий ход. Это же — бюджет,
// от которого подсказка «нажмите /new» считает заполнение (agent/lib/context-fill.ts):
// выше этой доли контекст не растёт, eve его сжимает.
export const COMPACTION_THRESHOLD_PERCENT = 0.7;
